// Vitest setup file (optional)
// You can add global mocks, setup, or configurations here.
// For example, mocking global browser APIs if needed for some tests.

// import '@testing-library/jest-dom'; // if you want to use jest-dom matchers
// import { vi } from 'vitest';

// Example: Mocking a global function or module if needed across many tests
// vi.mock('some-module', () => ({
//   default: vi.fn(),
//   someNamedExport: vi.fn(),
// }));
